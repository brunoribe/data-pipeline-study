atlas_insight:
  target: local
  outputs:
    local:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '/opt/project/storage/warehouse/analytics.duckdb') }}"
      schema: main
      threads: 4

    motherduck:
      type: duckdb
      path: "md:{{ env_var('MOTHERDUCK_DATABASE', 'atlas_insight') }}"
      schema: main
      threads: 4
      token: "{{ env_var('MOTHERDUCK_TOKEN', '') }}"